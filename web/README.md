# Future web frontend

The GitHub README is the primary interface.

`web/index.html` is a tiny static preview that reads `data/jobs.json`. It is
intentionally unfinished so it does not compete with the README experience.
